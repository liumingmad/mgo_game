#include "timer.h"

void GameTimerCallback::onTrigger() {
    // std::cout << "GameTimerCallback onTrigger()" << std::endl;

    // 检查所有room，服务器主动发起的推送

    // 1.检查落子是否超时

    // 2.对手离线

    // 3.对手上线
}